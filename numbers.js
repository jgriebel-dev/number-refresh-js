const secsPerMin = 60;
const minsPerHr = 60;
const HrPerDay = 24;
const daysPerWeek = 7;
const weeksPerYear = 52;


// const secondsPerDay = secsPerMin * minsPerHr * HrPerDay;
// console.log(`there are ${secondsPerDay} in day`);

const yearsAlive = 29
const secondsYears = secsPerMin * HrPerDay * weeksPerYear * yearsAlive
console.log(`ive been alive more than ${secondsYears} seconds`)